# Skill Home Web UI 修复验收记录

验收日期：2026-03-23  
验收对象：[http://47.122.112.210:8080](http://47.122.112.210:8080)

## 已关闭问题

- 首页已移除 `Highlights` 模块，首页结构收敛为 Hero、推荐技能、简化起步路径和 FAQ。
- 技能中心已移除 `即时预览` 按钮和右侧预览栏，只保留 `详情与安装` 与 `复制引用`。
- 移动端头部已压缩为 `品牌 + 搜索 + 菜单`，登录注册已移入菜单。
- 移动端技能中心已改为结果优先，筛选器折叠到 `筛选` 按钮后面。
- 技能详情页已重构首屏，操作区与关键信息合并到右侧摘要栏。
- 版本表不再显示错误的 `1年1月1日 08:06`，零值时间回退为 `未记录`。
- 复制操作已补统一 toast 反馈，并增加剪贴板失败兜底。
- 已补 `favicon.svg`，浏览器不再额外请求缺失图标资源。

## 验收结果

- `npm run build` 通过。
- 本地 `vite preview` 下完成桌面与移动端浏览器回归。
- 线上已重新部署，首页资源为 `index-CMiNUl1_.js` 与 `index-DDDQjtv5.css`。
- 线上浏览器断言结果：
  - 首页无 `Highlights`
  - 技能中心无 `即时预览`
  - 技能中心移动端存在 `筛选` 折叠入口
  - 详情页无错误零值时间
  - 移动端无横向溢出

## 第二轮收口

- 技能中心默认改为高密度列表视图，列表项已补充描述、标签和四项关键元信息，列表页不再依赖表头才能理解内容。
- 筛选区已补充 `常用标签` 快捷入口与当前筛选摘要，减少用户在侧栏和结果区之间来回对照。
- 详情页右侧六张指标卡已收敛为紧凑摘要块，首屏信息密度明显高于上一版。
- 全站间距、圆角、按钮和输入框体积已再压缩一轮，视觉节奏更接近产品控制台而不是展示页。

### 第二轮验证结果

- 本地重新构建通过，最新产物为 `index-CBy2eiEW.js` 与 `index-DH6Rbqjr.css`。
- 本地浏览器断言结果：
  - 首页无 `Highlights`
  - 技能中心默认列表模式渲染 `10` 条 `catalog-row`
  - 技能中心已渲染 `40` 个 `catalog-row__fact`
  - 技能中心仍无 `即时预览`
  - 移动端无横向溢出
  - 详情页存在 `6` 个 `detail-fact`
  - 详情页无 `1年1月1日` 错误时间
- 线上已再次部署，切换批次为 `DEPLOY_OK:20260323090519`。
- 线上验证结果：
  - 首页与详情路由都已引用 `index-CBy2eiEW.js` 和 `index-DH6Rbqjr.css`
  - 技能中心移动端无横向溢出
  - 详情页已渲染 `6` 个 `detail-fact`
  - 详情页版本发布时间仍正确回退为 `未记录`

## 第三轮收口

- 首页新增 `Quick access` 入口区，把搜索、安装、发布三条主路径前置，减少先读说明再行动的成本。
- 首页推荐区改为 `重点 skill + 最近更新 + 高频主题` 的双栏结构，不再继续堆 4 张同尺寸推荐卡。
- 首页起步路径改为更紧凑的三步列表，并把“第一次装不上”的操作提示收进右侧说明卡。
- 安装引导新增 `直接安装命令` 主命令区，先暴露 install 命令，再把 `doctor`、`search`、`pull` 作为辅助动作展示。
- 安装页新增示例目标卡，进入安装页后可直接看到当前示例 skill、引用名和跳转详情动作。
- 首页高频主题按钮已改为写入前端筛选状态后再进入技能中心，避免 query 路由不生效。

### 第三轮验证结果

- 本地重新构建通过，最新产物为 `index-Dw4qdJwj.js` 与 `index-XhNZllML.css`。
- 本地浏览器断言结果：
  - 首页存在 `3` 个 `quick-action-card`
  - 首页存在 `1` 个 `spotlight-skill-card`
  - 安装页存在 `1` 个 `install-target-card`
  - 安装页存在 `1` 个 `install-guide__primary-command`
  - 安装页正文包含 `直接安装命令`
  - 首页与安装页移动端均无横向溢出
- 线上已再次部署，切换批次为 `DEPLOY_OK:20260323091346`。
- 线上验证结果：
  - 首页与安装页都已引用 `index-Dw4qdJwj.js` 和 `index-XhNZllML.css`
  - 首页存在 `3` 个 `quick-action-card`
  - 首页存在 `1` 个 `spotlight-skill-card`
  - 安装页存在 `1` 个 `install-target-card`
  - 安装页存在 `1` 个 `install-guide__primary-command`
  - 首页移动端无横向溢出

## 相关文档

- 问题基线报告：[skill-home-web-ui-test-report-2026-03-23.md](./skill-home-web-ui-test-report-2026-03-23.md)
