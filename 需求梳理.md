# AI Skill 管理工具链需求梳理

## 1. 核心目标

建立**"本地管理工具 + 云端注册中心"**体系，打破 Claude Code、GitHub Copilot、Codex、Cursor 和 Antigravity 之间的技能（Skills/Rules）隔离，实现**"一次编写，到处同步"**。

---

## 2. 技能标准定义 (Source of Truth)

所有技能统一采用 **SKILL.md** (Agent Skills) 开放标准：

| 组成部分 | 说明 |
|---------|------|
| 目录结构 | 每个技能为一个独立文件夹 |
| 元数据 | 必须包含 YAML Frontmatter（name, description） |
| 执行层 | 支持可选的 `scripts/` (Python/Bash) 和 `references/` 文件夹 |

---

## 3. 本地管理工具需求 (CLI Manager)

该工具负责物理文件的发现、分发与格式转换。

### 3.1 目录自动发现
扫描项目根目录至 Git 根目录的所有配置文件夹（如 `.claude/`、`.cursor/`、`.agent/`）。

### 3.2 统一映射机制 (Mapping)

**核心逻辑**：采用符号链接 (Symlink) 或物理同步。

| 工具 | 项目级映射路径 | 全局级映射路径 |
|-----|---------------|---------------|
| Claude Code | `.claude/skills/` | `~/.claude/skills/` |
| Copilot | `.github/skills/` | `~/.copilot/skills/` |
| OpenAI Codex | `.agents/skills/` | `~/.agents/skills/` |
| Antigravity | `.agent/skills/` | `~/.gemini/antigravity/skills/` |
| Cursor | `.cursor/rules/` | N/A（集成设置面板） |

### 3.3 智能转换器 (Transpiler)
- 当同步到 Cursor 时，自动将 SKILL.md 转换为 `.mdc` 格式
- 自动从 SKILL.md 提取描述信息填充到 .mdc 的 description 字段

### 3.4 冲突解决
遵循优先级：**项目级技能 > 全局级技能**

### 3.5 双模模式（Codex 建议）
由于 Cursor 和 Claude Code 对符号链接的支持可能因版本更新而失效：
- **优先尝试**：Symlink
- **自动降级**：若检测到 IDE 无法读取，则切换为"文件物理镜像同步（Mirror Sync）"

---

## 4. 云端注册中心需求 (Registry Server)

类似 npm，负责技能的版本化托管。

### 4.1 包管理规范

| 规范项 | 说明 |
|-------|------|
| 命名空间 | 支持 `@username/skill-name` |
| 版本控制 | 强制执行语义化版本 (SemVer) |

### 4.2 API 接口

| 接口 | 方法 | 功能描述 |
|-----|------|---------|
| `/search` | GET | 支持根据 description 进行语义搜索 |
| `/publish` | POST | 接收本地工具上传的技能压缩包（Tarball） |
| `/install/:name` | GET | 下载特定版本的技能包 |

### 4.3 安全审计 (Security Gate)

| 审计类型 | 说明 |
|---------|------|
| 静态扫描 | 自动检测脚本中的恶意命令（如 `rm -rf /`） |
| 意图检查 | 识别明显的提示词注入（Prompt Injection）攻击模式 |

---

## 5. 执行与协同流程 (User Flow)

```
┌─────────────────────────────────────────────────────────────┐
│  1. 开发技能                                                  │
│     用户在本地 ~/.agent-skills/ 开发一个新的代码审查技能         │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  2. 推送云端                                                  │
│     执行 skill push，注册中心验证通过后存入数据库并发布版本       │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  3. 多工具同步                                                │
│     在另一个项目中执行 skill sync --all                       │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  4. 自动注入                                                  │
│     工具自动在项目下创建：                                     │
│     • .claude/skills/code-reviewer/                          │
│     • .cursor/rules/code-reviewer.mdc                        │
│     • .agent/skills/code-reviewer/                           │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  5. Agent 调用                                                │
│     无论用户唤起哪个 IDE，Agent 都能通过本地路径读取到相同的       │
│     专家级指令                                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 关键设计亮点

| 亮点 | 说明 |
|-----|------|
| **统一标准** | SKILL.md 作为单一事实来源 (Source of Truth) |
| **透明同步** | 用户无感知完成多工具配置 |
| **安全闸门** | 发布前自动审计脚本安全性 |
| **容错机制** | Symlink/物理同步双模式适配不同 IDE |
| **语义搜索** | 基于 description 的智能技能发现 |
| **命名空间** | 支持用户/组织级别的技能隔离 |

---

## 7. 待明确问题

1. 注册中心的数据库选型（PostgreSQL / MongoDB？）
2. 用户认证与授权机制（OAuth / JWT？）
3. 技能版本依赖管理（是否支持技能之间的依赖？）
4. 本地缓存策略（如何避免重复下载？）
5. 技能更新通知机制（如何告知用户有新版本？）

---

## 8. 平台社交化与用户统计需求

### 8.1 当前功能盘点

| 能力 | 当前状态 | 说明 |
|------|----------|------|
| 评分/评论 | 已有 | 后端支持登录用户对 skill 做 1-5 星评分和短评，并维护 `rating_sum`、`rating_count`；前端详情页可提交评分。 |
| 点赞 | 未见独立功能 | 当前没有 `like`/`favorite` 数据表、点赞接口、点赞计数字段或前端点赞按钮。评分不能直接等同点赞。 |
| 分享 | 仅有复制能力 | 前端支持复制引用名、安装命令、下载链接，搜索筛选条件写入 URL；没有 Web Share、分享弹层、分享记录或分享计数。 |
| 下载量 | 已有 | `download_count` 在下载接口命中时递增；当前更接近下载次数，不等同真实安装次数。 |
| 安装量 | 未见独立统计 | CLI `install` 会先 `pull`/下载包再安装到目标 IDE，本身没有独立上报安装成功事件。 |
| 社区标签 | 部分具备 | 后端和前端已有社区 tag 相关模型/处理函数/页面逻辑，但主服务路由中未看到对应接口挂载，需要补齐或确认是否刻意隐藏。 |
| 用户基础统计 | 部分具备 | 前端账号页基于 `GET /user/skills` 仅做本地技能数量、公有/私有数量统计；后端没有统一用户统计接口。 |

### 8.2 目标

把 Skill Home 从“发布/搜索/安装工具”增强为具备基础社交信号的平台：

- 每个 skill 可以展示作者信息、点赞数、评分、下载/安装趋势。
- 每个用户可以展示中文名、用户名、产出 skill 数、获得点赞数、安装数等贡献指标。
- 分享行为具备明确入口，必要时可被统计和运营分析。

### 8.3 注册与用户信息

注册用户新增必填字段：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `username` | string | 是 | 登录/命名空间用英文用户名，保持现有规则。 |
| `display_name_zh` | string | 是 | 用户中文名，注册必填，用于页面展示和作者信息。 |
| `email` | string | 是 | 登录邮箱。 |
| `password` | string | 是 | 登录密码。 |

落地要求：

- `users` 表增加 `display_name_zh` 字段，建议长度 32 或 64，非空。
- 注册接口 `POST /api/v1/auth/register` 校验中文名必填。
- 登录响应、`GET /api/v1/user`、管理员用户列表返回中文名。
- 前端注册表单增加“中文名”必填输入框。
- CLI 登录后展示可继续保留 `username`，后续如展示用户资料再补中文名。

### 8.4 Skill 元信息增加用户名

Skill 对外元信息需要明确包含作者用户信息：

| 场景 | 需求 |
|------|------|
| 列表/搜索结果 | 返回 `owner.username`、`owner.display_name_zh`，或返回扁平字段 `owner_username`、`owner_display_name_zh`。 |
| 详情页 | 保持 `owner` 对象，同时补充中文名；页面显示“中文名 / @username”。 |
| CLI `search/info` | 至少展示 `@username`，如有中文名则展示中文名。 |
| skill manifest | 不建议把用户名作为用户可编辑的事实来源；发布时由服务端根据登录用户写入响应元信息，避免伪造作者。 |

建议数据来源以 `owner_id -> users` 为准，`skills.author` 仅作为历史兼容字段或外部作者备注。

### 8.5 点赞需求

新增 skill 点赞能力：

| 能力 | 规则 |
|------|------|
| 点赞/取消点赞 | 登录用户可对公开 skill 点赞或取消点赞；一个用户对一个 skill 只能有一个点赞记录。 |
| 计数 | skill 返回 `like_count`，登录态额外返回 `viewer_liked`。 |
| 排序 | skill 列表支持按点赞数排序，可作为推荐排序因子之一。 |
| 权限 | 私有 skill 仅 owner/管理员可见时允许内部点赞，或第一阶段只支持公开 skill。 |
| 审计 | 记录 `skill.like`、`skill.unlike` 审计事件。 |

建议数据模型：

- 新表 `skill_likes(id, skill_id, user_id, created_at)`。
- 唯一索引 `(skill_id, user_id)`。
- `skills.like_count` 作为冗余计数字段，或查询聚合后缓存；第一阶段建议冗余，便于列表排序。

### 8.6 分享需求

分享分为两个层次：

| 层次 | 需求 |
|------|------|
| 基础分享 | 详情页提供“分享”入口，优先调用浏览器 Web Share API，不可用时复制详情页链接。 |
| 统计分享 | 如果需要运营指标，新增分享事件接口记录分享渠道、skill、用户/匿名访客、时间。 |

建议第一阶段实现基础分享，不强制做分享计数；如果产品需要排行榜或增长分析，再增加 `skill_share_events`。

### 8.7 安装数量与下载数量口径

需要区分三个指标：

| 指标 | 口径 | 当前状态 |
|------|------|----------|
| `download_count` | 下载接口被访问次数 | 已有 |
| `install_count` | CLI 成功安装到目标 IDE 的次数 | 未有 |
| `unique_install_users` | 去重安装用户数 | 未有 |

建议新增 CLI 安装成功上报：

- `POST /api/v1/skills/:namespace/:name/install-events`
- 参数：`version`、`target`（codex/claude/cursor/copilot/openclaw）、`install_mode`、`client_version`
- 登录态记录 `user_id`；匿名安装可记录匿名事件但不进用户贡献统计。
- 服务端维护 `install_count`，用户统计可按 owner 汇总其 skill 的安装总数。

### 8.8 用户信息统计

新增用户统计接口：

```http
GET /api/v1/users/:username/stats
GET /api/v1/user/stats
```

返回建议：

```json
{
  "username": "testuser",
  "display_name_zh": "测试用户",
  "skill_count": 8,
  "public_skill_count": 6,
  "total_like_count": 120,
  "total_rating_count": 34,
  "average_rating": 4.6,
  "total_download_count": 980,
  "total_install_count": 420
}
```

统计口径：

- `skill_count`：该用户拥有的全部未删除 skill。
- `public_skill_count`：公开且未删除 skill。
- `total_like_count`：该用户拥有的 skill 的点赞数求和。
- `total_download_count`：该用户拥有的 skill 的 `download_count` 求和。
- `total_install_count`：该用户拥有的 skill 的安装事件数求和。
- `average_rating`：建议按所有评分记录加权计算，而不是对各 skill 均分再平均。

### 8.9 前端展示需求

| 页面 | 需求 |
|------|------|
| 注册页 | 新增中文名必填项。 |
| 顶部用户菜单 | 可展示中文名，用户名作为次级标识。 |
| skill 列表卡片 | 展示作者中文名/用户名、点赞数、安装数、评分。 |
| skill 详情页 | 增加作者信息区、点赞按钮、分享按钮、安装数。 |
| Settings Stats 页 | 提供独立数据统计页，展示产出 skill、获赞、安装、下载、评分，并列出各 skill 表现。 |
| 搜索/筛选 | 支持按点赞数、安装数排序。 |

### 8.10 API 草案

```http
POST   /api/v1/skills/:namespace/:name/like
DELETE /api/v1/skills/:namespace/:name/like
POST   /api/v1/skills/:namespace/:name/share-events
POST   /api/v1/skills/:namespace/:name/install-events
GET    /api/v1/user/stats
GET    /api/v1/users/:username/stats
```

列表与详情响应新增字段：

```json
{
  "owner": {
    "username": "testuser",
    "display_name_zh": "测试用户"
  },
  "like_count": 12,
  "viewer_liked": true,
  "install_count": 45
}
```

### 8.11 分阶段建议

1. 第一阶段：中文名必填、skill 元信息返回作者、用户统计接口、前端独立 Stats 统计页展示。
2. 第二阶段：点赞/取消点赞、点赞数展示、按点赞排序。
3. 第三阶段：安装成功上报、安装数展示、用户安装贡献统计。
4. 第四阶段：分享入口；如有运营诉求，再补分享事件统计。

### 8.12 待确认问题

1. 中文名是否必须为中文字符，还是只要求展示名必填即可？
2. 点赞是否允许给自己的 skill 点赞？
3. 私有 skill 是否支持点赞、安装计数和用户统计展示？
4. 安装数是否只统计 CLI `install` 成功，还是 `pull`/下载 ZIP 也算安装？
5. 分享是否需要计数；如果需要，是否统计匿名用户？
6. 用户统计页是否公开，还是仅本人和管理员可见？

## 9. 用户权限管理需求调研

### 9.1 当前功能盘点

| 能力 | 当前状态 | 说明 |
|------|----------|------|
| 登录认证 | 已有 | 支持 JWT 与 API Key；中间件会按 token 查用户，并拒绝已停用用户。 |
| 用户角色 | 部分具备 | `users` 表已有 `is_admin`、`is_super_admin` 两个布尔字段，没有独立角色/权限表。 |
| Skill 所有者权限 | 已有 | owner 可更新、删除自己的 skill 和版本；super admin 可管理任意 skill/版本。 |
| 目录推荐管理 | 已有 | `admin` 与 `super_admin` 可调整公开 skill 推荐状态。 |
| 用户管理 API | 部分具备 | super admin 可通过 `/api/v1/admin/users` 列表查看用户，通过 `PUT /api/v1/admin/users/:id` 修改启停、管理员标记、超管标记和密码。 |
| 超级管理员引导 | 已有 | 启动时可通过 `SKILL_HOME_AUTH_BOOTSTRAP_SUPER_ADMIN` 将指定用户名提升为超管。 |
| 审计日志 | 部分具备 | skill、评分、用户管理等关键动作有 audit log，但缺少面向管理员的全局审计入口和权限变更专属筛选视图。 |
| 前端权限入口 | 部分具备 | Web 已按 `is_admin`/`is_super_admin` 控制推荐设置、skill 设置入口；暂未发现完整用户管理页面。 |
| API Key 权限 | 较粗 | API Key 当前等价于用户身份，没有 scope、最小权限、用途限制或只读 Key。 |

### 9.2 现有权限模型

| 角色/身份 | 权限范围 |
|-----------|----------|
| 匿名用户 | 浏览公开 skill、搜索、下载公开版本、记录匿名安装/分享事件。 |
| 注册用户 | 发布 skill、管理自己的 skill/版本、评分、点赞、管理自己的 API Key、查看自己的技能/统计/审计日志。 |
| Skill owner | 对自己拥有的 skill 做元数据更新、公开/私有切换、弃用、删除、发布/删除版本。 |
| Admin | 管理公开目录推荐状态；不能管理用户，不能修改非自己拥有的 skill 元数据。 |
| Super admin | 管理任意 skill/版本；管理用户启停、管理员/超管标记、重置密码；可被启动配置引导。 |

当前模型适合早期平台，但更像“硬编码角色开关”，还不是完整权限管理产品。

### 9.3 主要缺口

| 缺口 | 影响 |
|------|------|
| 缺少权限矩阵文档和统一授权策略 | 新增接口容易各自判断，后续权限行为不一致。 |
| 缺少 Web 用户管理页 | super admin 虽有 API，但运营/管理员无法在 Web 完成用户启停、角色调整、密码重置。 |
| 缺少用户搜索、分页、筛选 | 用户量增长后 `/admin/users` 全量列表不可持续。 |
| 缺少危险操作保护 | 需要防止停用/降级最后一个 super admin、误停用自己、随意提升超管等高风险操作。 |
| 缺少 API Key scope | CLI/API Key 一旦泄露即拥有账号全部权限，不利于 CI、只读搜索、自动化发布等场景。 |
| 缺少用户自助资料与密码管理 | 当前用户不能自行修改中文名、头像、密码；只能注册时写入或由超管重置密码。 |
| 缺少全局审计管理视图 | 权限变更、用户停用、角色调整、API Key 创建/撤销缺少集中审计检索。 |
| 缺少权限错误的产品化反馈 | Web/CLI 主要展示接口错误；需要区分未登录、账号停用、角色不足、资源不可见。 |

### 9.4 建议目标

把权限管理从“后端具备少量 admin API”升级为可运营、可审计、可扩展的用户权限体系：

- 管理员能在 Web 中查看用户、筛选状态/角色、调整角色和启停账号。
- 平台有清晰角色定义和权限矩阵，后端授权逻辑和前端入口一致。
- 高风险权限变更有保护规则和审计记录。
- API Key 支持 scope，满足 CLI、CI、只读检索等不同权限边界。
- 普通用户能管理自己的基础资料和密码。

### 9.5 MVP 范围建议

第一阶段不建议立刻引入通用 RBAC 表。基于当前 `is_admin` / `is_super_admin` 继续做“角色产品化 + 安全护栏”，成本最低且能覆盖当前需求。

| 模块 | MVP 需求 |
|------|----------|
| 用户管理 API | `/admin/users` 支持分页、搜索、角色/状态筛选；保留 `PUT /admin/users/:id`，补充明确校验。 |
| Web 用户管理 | 新增 Settings/Admin 用户管理页：用户列表、角色标识、启停开关、admin/super admin 调整、密码重置入口。 |
| 安全护栏 | 禁止停用自己；禁止移除最后一个 super admin；提升 super admin 需要当前用户已是 super admin；密码重置最小长度与审计。 |
| 权限矩阵 | 在 API/README/需求文档中固化角色到能力的矩阵。 |
| 审计 | 权限变更写入 `admin.user.update`，前端展示最近权限变更；后端支持按 action/user 筛选全局审计。 |
| 前端入口 | 仅 super admin 展示“用户管理”；admin 只展示目录推荐相关能力。 |
| 测试 | 覆盖非超管禁止用户管理、超管可更新、最后超管保护、自停用保护、分页搜索筛选。 |

### 9.6 后续增强建议

| 阶段 | 建议能力 | 说明 |
|------|----------|------|
| 第二阶段 | API Key scope | 增加 `scopes` 字段，如 `skills:read`、`skills:write`、`skills:delete`、`user:read`、`admin:users`；CLI login 自动创建默认全权限 CLI key。 |
| 第三阶段 | 用户自助安全 | 当前用户修改中文名/头像/密码、查看登录/Key 使用记录、撤销所有 API Key。 |
| 第四阶段 | 真 RBAC | 如果出现团队、组织、空间、审核员、只读运营等复杂场景，再引入 `roles`、`permissions`、`user_roles` 或 resource-scoped grants。 |
| 第五阶段 | 组织/团队权限 | 支持团队 namespace、团队 owner、多人协作维护 skill。 |

### 9.7 API 草案

```http
GET    /api/v1/admin/users?page=1&per_page=20&q=zhang&role=admin&status=active
PUT    /api/v1/admin/users/:id
GET    /api/v1/admin/audit-logs?action=admin.user.update&user_id=:id
PUT    /api/v1/user/profile
PUT    /api/v1/user/password
```

`PUT /api/v1/admin/users/:id` 请求建议保持兼容：

```json
{
  "is_active": true,
  "is_admin": false,
  "is_super_admin": false,
  "password": "new-password"
}
```

第一阶段可以先不增加 `role` 字段，由后端根据布尔字段返回派生角色：

```json
{
  "id": "...",
  "username": "testuser",
  "display_name_zh": "测试用户",
  "email": "test@example.com",
  "role": "member",
  "is_active": true,
  "is_admin": false,
  "is_super_admin": false
}
```

### 9.8 验收标准草案

1. super admin 可以在 Web 用户管理页搜索/筛选用户，查看中文名、用户名、邮箱、角色、状态、创建时间。
2. super admin 可以启停用户、授予/取消 admin、授予/取消 super admin、重置密码，且所有变更写入审计日志。
3. 非 super admin 无法访问用户管理 API 和 Web 用户管理入口。
4. 系统禁止停用当前登录的 super admin 自己，禁止移除系统最后一个 super admin。
5. 被停用用户的 JWT 和 API Key 都不能继续访问认证接口。
6. 普通用户可以修改自己的中文名和密码，但不能修改自己的角色。
7. API 文档和 README 明确角色权限矩阵。

### 9.9 待确认问题

1. 第一阶段是否沿用 `is_admin`/`is_super_admin`，还是必须一次性改成通用 RBAC？
2. `admin` 的职责是否只管理目录推荐，还是也允许用户审核、内容下架、公开/私有切换？
3. 是否需要“审核员/运营/只读管理员”等中间角色？
4. 是否允许 super admin 停用自己或降级自己？建议默认禁止。
5. API Key scope 是否纳入本期，还是单独作为第二阶段？
6. 用户自助修改中文名/密码是否与用户权限管理同批交付？

### 9.10 本期落地记录

已按第一阶段 MVP 完成：

1. `/api/v1/admin/users` 支持分页、搜索、角色筛选和状态筛选，并返回派生 `role`。
2. `/api/v1/admin/users/:id` 增加安全护栏：禁止停用自己，禁止移除或停用最后一个有效 super admin；权限变更继续写入 `admin.user.update`。
3. 新增 `/api/v1/admin/audit-logs` 供 super admin 查看全局审计日志，支持 `action` 与 `user_id` 筛选。
4. 新增 `/api/v1/user/profile` 与 `/api/v1/user/password`，普通用户可自助修改中文名、头像和密码。
5. Web Settings 新增 Users 页面，仅 super admin 可见和可用，支持用户搜索/筛选、启停、角色调整、密码重置和近期审计展示。
6. `API.md`、`README.md`、`skill-home-server/README.md` 已补充权限矩阵和接口说明。

暂缓项：

1. API Key scope、通用 RBAC、组织/团队权限仍按后续阶段拆分。
2. 当前 Web 用户管理页展示最近审计记录，复杂审计检索视图可在下一阶段增强。
